# Sprint 7 — Close-Out Report (2026-05-27)

Generated by Hermes at end of S7-10. Reference doc — env var list,
endpoint map, follow-ups all live here.

## Cumulative LOC
36 files changed, 2,453 insertions(+), 14 deletions(-)

## Test path (227 → 319 across Sprints 6 / 6.5 / 7)
```
Sprint 6 baseline:  227 passed
After Sprint 6.5:   277 (+50)
S7-1:  277 → 277  (schema only)
S7-2:  277 → 284  (+7  billing/Stripe/webhook)
S7-3:  284 → 292  (+8  quotas)
S7-4:  292 → 297  (+5  CSV exports)
S7-5:  297 → 304  (+7  PDF reports)
S7-6:  304 → 311  (+7  API keys)
S7-7:  311 → 312  (+1  billing subscription GET)
S7-8:  312 → 318  (+6  admin dashboard)
S7-9:  318 → 319  (+1  cross-cutting integration)
Final: 319 passed, 3 xfailed, 0 failed
```

## Migrations
| Migration | Purpose |
|---|---|
| 0016 | billing_subscriptions — per-user tier + Stripe IDs + status |
| 0017 | api_keys — Enterprise API keys, SHA-256 hashed at rest |
| 0018 | exports — Audit trail for CSV/PDF exports |
| 0019 | ALTER users ADD COLUMN tier — denormalized for fast quota checks |

## Required env vars (all unset by default; production must set)
| Var | Purpose |
|---|---|
| STRIPE_API_KEY | Stripe test-mode key (refused if sk_live_) |
| STRIPE_WEBHOOK_SECRET | Webhook signing secret |
| STRIPE_PRICE_ID_BASIC | Basic tier ($8/yr) |
| STRIPE_PRICE_ID_LIFETIME | Lifetime tier ($108) |
| STRIPE_PRICE_ID_ENTERPRISE | Enterprise tier ($1000/yr) |

## New endpoints
| Endpoint | Method | Auth | Quota |
|---|---|---|---|
| /billing/subscription | GET | current_user | none |
| /billing/checkout-session | POST | current_user | valid tier |
| /billing/portal-session | POST | current_user | existing customer |
| /billing/webhook | POST | Stripe signature | none |
| /exports/expiry.csv | GET | current_user | Basic+ |
| /patents/{id}/report | POST | current_user | Lifetime+ |
| /account/api-keys | POST | current_user | Enterprise |
| /account/api-keys | GET | current_user | own keys |
| /account/api-keys/{id} | DELETE | current_user | own key |
| /admin/users | GET | require_admin | admin only |
| /admin/users/{id}/tier | POST | require_admin | admin only |
| /admin/exports | GET | require_admin | admin only |

## New frontend pages
- `/account/billing` — tier badge, upgrade plans, Stripe checkout, manage subscription
- `/admin` — Users + Exports tabs, tier override panel

## Outstanding follow-ups (close in production-readiness or post-V1)
| Item | Priority |
|---|---|
| Stripe TEST MODE live smoke (env vars need to be set) | High |
| AUTH_SECRET_KEY < 32 bytes — regenerate with `openssl rand -base64 32` | Medium |
| Audit log table for admin tier overrides (currently stdout only) | Low |
| Webhook idempotency verification with Stripe CLI | Low |

## Discipline regressions corrected mid-sprint (lessons for future chunks)
- **S7-5**: claimed "build clean" without running pytest as the last step; "DB pool exhausted on full suite" reported but actual suite was clean on rerun. **Fix**: literal pytest tail must be pasted, freshly run.
- **S7-7**: smoke table had 2 inaccurate entries (a "500" that was actually 200 once isAuthenticated was fixed; a "Stripe error" that was actually a missing-cookie 401). **Fix**: smoke results must be facts about a fresh test, not historical observations.
- **S7-8**: 2 tests marked xfail with reason "require_admin dep restructuring needed" — that's a TODO, not a deferral reason. Frontend /admin returned 500 with the explanation "auth guard needs restructuring". **Fix**: pushed back; Hermes implemented standard require_admin dep, removed xfails, fixed stale .next cache. Page now 200s.

All three corrected; final 319 passed includes only legitimate tests.
