# V3 Development Status — Handoff Document

**Date:** June 4, 2026
**Production:** inventionindex8.com (Hetzner 188.245.85.248)
**Git:** github.com/andykeh710/patent-pulse (main branch)
**Current HEAD:** 575db9a "fix: use correct Docker image tags"

---

## 1. What's Built & Deployed

### Design Overhaul (Complete, Deployed)
- Dark/light theme system with CSS tokens
- Palantir-inspired palette (steel-blue accent `#6B8CFF`)
- All AI-slop effects removed (gradient text, glassmorphism, purple, glow)
- 15 design skills installed in `.agents/skills/` and `~/.hermes/skills/`
- ThemeToggle in MarketingNav + TopNav
- Responsive, dark mode by default with light mode option

### Operational Hardening (Round 5 — Complete, Deployed)
- CI/CD auto-deploy: `.github/workflows/ci.yml` with deploy job (needs GitHub Secrets: DEPLOY_KEY, DEPLOY_HOST, RESEND_API_KEY)
- Sentry error tracking: backend (`app/observability/sentry.py`) + frontend (`sentry.*.config.ts`)
- Sentry smoke test: `/_sentry-test` endpoint
- Health endpoint: `/api/v1/health` with DB, Redis, Resend, alembic_head probes
- global-error.tsx: React render error boundary → Sentry
- Database backup: `app/tasks/backup.py` (Celery beat, 03:00 UTC daily)
- Runbooks: `.hermes/runbooks/deploy.md`, `restore-db.md`, `andy-actions-v3.md`

### Production Stability Fixes (Tier 0 — Complete, Deployed)
- Production Dockerfile: multi-stage `next build` + `next start` (no more `npm run dev`)
- docker-compose.prod.yml: removes dev volume mount, production command
- Docker images pinned: `pgvector/pgvector:pg16`, `redis:7.2-alpine`, `node:20.18-alpine`
- Duplicate React key errors fixed (trends page, companies page, assignee lists)
- Ruff lint: all 4 errors fixed (missing `timezone` import in ai_models.py)
- Deploy runbook written and verified

### Data Depth (Round 6 — Code Written, Needs Deploy + Migration)
- PatentsView API provider: `app/ingestion/patentsview_client.py` (free USPTO abstracts + claims)
- PatentsView backfill: `app/tasks/patentsview_backfill.py` (4x/day, 200/batch)
- BigQuery backfill: `app/tasks/bigquery_backfill.py` (daily 04:30 UTC, 500/batch)
- Citation/family diagnostic: `app/tools/diagnose_citations.py`
- Tag task circuit breaker: already existed in `app/tasks/tag.py`
- Admin health endpoint: already existed in `app/api/v1/admin.py` `/system-health`

### Weekly Briefing Email (Round 7 — Code Written, Needs Deploy + Test)
- Email templates: `app/email/templates/weekly_briefing.html` + `.txt`
- Renderer: `app/email/weekly_briefing.py` — briefing items → email
- Resend webhook: `app/api/v1/webhooks.py` — bounce/complaint handling
- Public unsubscribe: `GET /unsubscribe/{token}` — one-click, no auth, JWT-based
- Email preferences: `GET/PUT /api/v1/account/email-preferences`
- Preferences UI: `frontend/.../account/email-preferences/page.tsx`
- Fan-out task: `app/tasks/send_weekly_digest.py` (updated with JWT unsubscribe tokens)

### Stripe Billing (Round 8 — Code Written, Needs Deploy)
- Billing page: `frontend/.../account/billing/page.tsx`
- Legal pages: `/(marketing)/privacy/`, `/terms/`, `/refund/`
- Stripe infrastructure already existed: checkout, portal, webhook in `app/api/v1/billing.py`
- Stripe client: `app/billing/stripe_client.py`
- Billing models: `app/core/billing_models.py`

### AI For You + News (Round 9 — Code Written, Needs Migration + Deploy)
- User tracking: `UserViewEvent`, `UserEmbedding` models
- Recommendations: `app/services/recommendations.py` (cosine similarity, pgvector)
- For You endpoint: `GET /api/v1/today/for-you`
- News models: `NewsItem`, `NewsPatentLink`
- News ingestion: `app/tasks/news_ingestion.py` (15-min cadence, 7 RSS feeds)
- News-patent linking: embedding similarity → `NewsPatentLink` rows

---

## 2. Production State

### What's Running
| Container | Status | Notes |
|-----------|--------|-------|
| backend | Healthy | `/api/v1/health` returns 200 |
| frontend | Running | HTTP 200, production build (`next start`) |
| worker | Running | DeepSeek key fixed, processing backlog |
| db | Healthy | pgvector/pgvector:pg16 |
| redis | Healthy | redis:7.2-alpine |
| caddy | Running | Reverse proxy |

### Data Completeness
| Metric | Coverage | Notes |
|--------|----------|-------|
| Abstract coverage | ~15% | PatentsView + BigQuery backfills will improve this |
| Claims coverage | ~3% | PatentsView will improve this |
| Tag coverage | ~3% | Worker now processing with real DeepSeek key |
| Figure backfill | 5,000/64,231 | Backfill partially run, needs completion |
| Forward citations | ~22 | Diagnostic tool built, needs debugging |
| Family members | 0 | Diagnostic tool built, needs debugging |
| Companies page | "0 of 0" | Supplier normalization not populated |

### Known Issues
1. **Companies "0 of 0"**: Supplier normalization data not populated — needs backend investigation
2. **Expiry page empty**: Worker was unhealthy (DeepSeek key placeholder) — now fixed, data populating
3. **Worker was unhealthy**: Fixed by updating DEEPSEEK_API_KEY in app.env
4. **Resend API key leaked in chat**: Must rotate immediately

---

## 3. What Needs Migration

The Round 9 models need an Alembic migration for these new tables:
- `user_view_events`
- `user_embeddings`
- `news_items`
- `news_patent_links`

Commands:
```bash
ssh root@188.245.85.248 "cd /opt/invention-index-8 && docker compose exec backend alembic revision --autogenerate -m 'round9_user_embeddings_news'"
ssh root@188.245.85.248 "cd /opt/invention-index-8 && docker compose exec backend alembic upgrade head"
```

---

## 4. Andy Action Items (Tier 0-2)

### 🔴 NOW
- Rotate Resend API key (leaked in Hermes chat history)
- Finish figure backfill: `docker compose exec backend python -c "import asyncio; from app.tasks.backfill_figures import backfill_figure_urls; print(asyncio.run(backfill_figure_urls(limit=60000)))"`
- Deploy latest code: `git pull && docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build backend worker beat frontend`
- Run alembic migration for Round 9 models

### 🟡 This Week
- UptimeRobot monitor on inventionindex8.com
- Sentry DSNs on server
- Stripe business verification (dashboard.stripe.com/account)
- DNS SPF/DKIM/DMARC for Resend deliverability
- Test weekly briefing email in dev mode
- Test DB restore drill

### 🟢 Before Launch
- Stripe LIVE products + webhook
- Email preferences tested end-to-end
- Welcome email on signup
- GDPR data export endpoint
- Empty state audit across all pages
- First-time user experience audit

---

## 5. V2-V3 Implementation Plan

See `.hermes/plans/2026-06-03_v2-vision.md` for full roadmap.

### Phase Order (from V2 plan):
1. **Semantic Search** — DeepSeek embeddings + pgvector (foundation for everything)
2. **Research Papers** — OpenAlex, arXiv, Semantic Scholar ingestion
3. **Paper-to-Patent Matching** — cross-linking research to IP
4. **Chatbot** — SSE streaming, RAG, tool-calling
5. **Knowledge Graph** — D3.js visualization
6. **LLM Wiki** — git-tracked markdown knowledge base
7. **Community Layer** — feature requests, upvoting, boards
8. **Polish** — onboarding, SEO, export/reports, onboarding

### Tiered Priority (from 117-item list):
**Tier 0** (items 1-8): Production stability — 90% done
**Tier 1** (items 9-22): Foundation stability + data quality — 30% done
**Tier 2** (items 23-60): Email, Stripe, legal, onboarding — 40% done
**Tier 3** (items 61-117): Analytics, marketing, perf, security, docs — 5% done

---

## 6. Key Files Reference

| Area | Path |
|------|------|
| CI/CD | `.github/workflows/ci.yml` |
| Docker (dev) | `docker-compose.yml` |
| Docker (prod) | `docker-compose.prod.yml` |
| Frontend Dockerfile | `frontend/Dockerfile` |
| Backend Dockerfile | `backend/Dockerfile` |
| Design tokens | `frontend/src/styles/tokens.css` |
| Theme provider | `frontend/src/lib/ThemeProvider.tsx` |
| Error boundary | `frontend/src/app/global-error.tsx` |
| Health endpoint | `backend/app/api/health.py` |
| Stripe billing | `backend/app/api/v1/billing.py` |
| Stripe client | `backend/app/billing/stripe_client.py` |
| Email sender | `backend/app/email/sender.py` |
| Briefing email | `backend/app/email/weekly_briefing.py` |
| Briefing templates | `backend/app/email/templates/weekly_briefing.*` |
| Webhooks (Resend) | `backend/app/api/v1/webhooks.py` |
| Recommendations | `backend/app/services/recommendations.py` |
| News ingestion | `backend/app/tasks/news_ingestion.py` |
| PatentsView client | `backend/app/ingestion/patentsview_client.py` |
| PatentsView backfill | `backend/app/tasks/patentsview_backfill.py` |
| BigQuery backfill | `backend/app/tasks/bigquery_backfill.py` |
| DB backup | `backend/app/tasks/backup.py` |
| Tag circuit breaker | `backend/app/tasks/tag.py` |
| Celery beat config | `backend/app/tasks/celery_app.py` |
| New models (R9) | `backend/app/core/models.py` (UserViewEvent, UserEmbedding, NewsItem, NewsPatentLink) |
| Today/For You API | `backend/app/api/v1/today.py` |
| Email prefs API | `backend/app/api/v1/account.py` |
| Email prefs UI | `frontend/src/app/(app)/account/email-preferences/page.tsx` |
| Billing UI | `frontend/src/app/(app)/account/billing/page.tsx` |
| Privacy | `frontend/src/app/(marketing)/privacy/page.tsx` |
| Terms | `frontend/src/app/(marketing)/terms/page.tsx` |
| Refund | `frontend/src/app/(marketing)/refund/page.tsx` |
| Runbooks | `.hermes/runbooks/` |
| Plans | `.hermes/plans/` |
| Design skills | `.agents/skills/` (15 skills from emilkowalski, impeccable, Leonxlnx/taste-skill) |

---

## 7. Next Steps for New Agent

1. **Generate and run alembic migration** for Round 9 models (user_view_events, user_embeddings, news_items, news_patent_links)
2. **Deploy all pending code** to production
3. **Complete figure backfill** (59K remaining)
4. **Fix companies "0 of 0"** — investigate supplier normalization
5. **Test weekly briefing email** end-to-end
6. **Start V2 Phase 1: Semantic Search** — DeepSeek embeddings on all 64K patents
7. **Continue through the 117-item V3 checklist** by priority tier
